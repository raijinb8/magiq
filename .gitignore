# -----------------------------------------------------------------------------
# Environment Variables
# -----------------------------------------------------------------------------
# 通常、環境固有の設定ファイルはバージョン管理に含めません。
# .env.example などのテンプレートファイルは含めるのが一般的です。
.env
.env*.*local
*.local

# -----------------------------------------------------------------------------
# Dependencies / Build Output
# -----------------------------------------------------------------------------
# フロントエンドの依存関係とビルド成果物
/frontend/node_modules/
/frontend/dist/
/frontend/dist-ssr/ # もしSSRビルドの出力先がこれなら

# Supabase Edge Functions 内の依存関係 (もしあれば)
/supabase/functions/**/node_modules/

# ルートレベルや他の場所の可能性 (もし存在する場合。通常は上記でカバー)
# node_modules/
# dist/
# dist-ssr/

# npm / yarn / pnpm のキャッシュや一時ファイル
.npm/

# -----------------------------------------------------------------------------
# Logs
# -----------------------------------------------------------------------------
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log* # Lernaを使用している場合

# -----------------------------------------------------------------------------
# Supabase Specific
# -----------------------------------------------------------------------------
# Supabaseのローカル開発用ファイル
/supabase/.supabase/
/supabase/db/volumes/ # Supabase v0 の場合
/supabase/.branches/main/db/volumes/ # Supabase v1 以降の場合

# -----------------------------------------------------------------------------
# IDE / Editor Specific
# -----------------------------------------------------------------------------
# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
frontend/.vscode
supabase/.vscode
frontend/.prettier.js

# JetBrains IDEs (IntelliJ, WebStorm, etc.)
.idea/

# Visual Studio
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# -----------------------------------------------------------------------------
# OS Specific
# -----------------------------------------------------------------------------
# macOS
.DS_Store

# Windows
Thumbs.db

# -----------------------------------------------------------------------------
# Project Specific Files
# -----------------------------------------------------------------------------
# プロジェクト固有でバージョン管理に含めたくないファイル
# 例: Google Cloud Service Account Keyなど (実際にはより安全な方法で管理してください)
magiq-457412-1644a91ead49.json